name: Build and Push Docker Images

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Checkout BACKEND repository (this repo)
      - name: Checkout backend
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ---- FRONTEND CHECKOUT ----
      - name: Checkout frontend repo
        uses: actions/checkout@v4
        with:
          repository: MSaiPraveen/TeleEcom-Frontend      # <-- your correct repo
          path: frontend

      # ---- BACKEND BUILD ----
      - name: Build & Push Backend Image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            saipraveen919/teleecom-backend:latest
            saipraveen919/teleecom-backend:${{ github.sha }}
            saipraveen919/teleecom-backend:v5.1.0

      # ---- FRONTEND BUILD ----
      - name: Build & Push Frontend Image
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            saipraveen919/teleecom-frontend:latest
            saipraveen919/teleecom-frontend:${{ github.sha }}
            saipraveen919/teleecom-frontend:v5.1.0
